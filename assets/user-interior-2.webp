{
  "name": "verospace-landing",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "rm -rf public && mkdir -p public/assets && cp index.html styles.css script.js public/ && cp -R assets/. public/assets/"
  }
}
